[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile"

[deploy]
# Railway injeta PORT automaticamente — o server.js standalone lê de process.env.PORT.
# healthcheckPath omitido: Railway verifica se a porta está respondendo.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
