/**
 * Global test setup.
 *
 * Registers `@testing-library/jest-dom` custom matchers (e.g. `toBeInTheDocument`,
 * `toHaveAttribute`) so component tests in the Biblioteca_de_Componentes can use
 * them via Vitest's `expect`.
 */
import "@testing-library/jest-dom/vitest";
